#!/bin/sh
. $(dirname "$0")/send.config

# sudo systemctl daemon-reload
# sudo systemctl disable send@dev01
# sudo systemctl status  send@dev01
# sudo systemctl stop    send@dev01
# sudo systemctl start   send@dev01

$DPDKPY restore
echo 0 | tee /sys/module/vfio/parameters/enable_unsafe_noiommu_mode
# cat /sys/module/vfio/parameters/enable_unsafe_noiommu_mode

# bin

# 0000:01:00.0
# 'MT27800 Family [ConnectX-5] 1017'
# if=enp1s0np1 drv=mlx5_core unused=igb_uio,vfio-pci,uio_pci_generic

# cat /proc/meminfo | grep Huge
# cat /sys/kernel/mm/hugepages/hugepages-2048kB/nr_hugepages

sudo ip addr add $SIP dev $ETH
sudo ip link set $ETH up
